using System;

namespace Math
{
    public class Program
    {
        public static void Main(string[] args)
        {
            // el metodo Math.Max(x,y) sirve para buscar el mayor valor entre x y
            Math.Max(5, 10);
            // despues esta el .Min que es lo mismo pero con el menor
            Math.Min(5, 10);

            Math.Sqrt(x) // raiz de x

            // Math.Abs(x) el absoluto de x
            Math.Abs(-4.7);

            // Math.Round() redondea el valor del numero
            Math.Round(9.99);


        }
    }
}